"""Dashboard payload — seed data the Sentinel frontend renders.

For now this serves mock data shaped exactly like the UI needs. Later,
`seed.build_payload()` is replaced by data assembled from the real
intake/remediation/validation outputs; the schema stays put.
"""
